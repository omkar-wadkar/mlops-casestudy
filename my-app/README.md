# My-App (Sample DevOps Project)
This repository contains a sample application and DevOps pipeline files:
- Jenkinsfile (Declarative pipeline)
- Dockerfile
- Ansible playbook and inventory
- Kubernetes manifests (deployment + service)
- ML training script that logs to MLflow
- requirements.txt for Python dependencies
- .gitignore

Replace placeholder values (dockerhub username, repo URLs, credentials) before use.
